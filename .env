# Default (safe) API base URL.
# - Leave empty to use relative /api (works with Vite proxy locally, and with hosting rewrites/proxies if configured).
# - For local development, set VITE_API_BASE_URL in .env.local.
VITE_API_BASE_URL=https://act-one-backend-919741124920.asia-south1.run.app